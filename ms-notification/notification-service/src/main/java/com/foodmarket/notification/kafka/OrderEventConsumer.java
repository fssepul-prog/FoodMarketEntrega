package com.foodmarket.notification.kafka;

/**
 * Kafka removido del proyecto ya que no llego a abordarse en profundidad en clases.
 * Las notificaciones se consultan directamente via GET /notifications/{userId}
 * y se crean manualmente si se requieren pruebas.
 *
 * Este archivo se mantiene como placeholder para no romper la estructura de paquetes .
 * considerar esto para la futura progresion del sistema.
 */
public class OrderEventConsumer {
    // Sin implementacion — Kafka desactivado
}
